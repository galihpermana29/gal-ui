# Private npm registry configuration
# Uncomment and configure based on your registry (npm, GitHub Packages, or private registry)

# For npm private packages (default)
# registry=https://registry.npmjs.org/
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# For GitHub Packages
# @synergysg:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# For private registry (e.g., Verdaccio, Artifactory)
# registry=https://your-private-registry.com/
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# Always use exact versions for security
save-exact=true
